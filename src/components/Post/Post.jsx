import{ useEffect, useState } from 'react';
import './Post.css'

function Post({ postId, onRouteChange, isSignedIn, userId }) {
    return (
        <>users post there</> 
    )
}
export default Post;
